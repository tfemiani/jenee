'use strict';

describe('myApp.viewJira module', function() {

  beforeEach(module('myApp.viewJira'));

  describe('viewJira controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewJiraCtrl = $controller('ViewJiraCtrl');
      expect(viewJiraCtrl).toBeDefined();
    }));

  });
});
