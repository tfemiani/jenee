'use strict';

describe('myApp.viewMeeting module', function() {

  beforeEach(module('myApp.viewMeeting'));

  describe('viewMeeting controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewMeetingCtrl = $controller('ViewMeetingCtrl');
      expect(viewMeetingCtrl).toBeDefined();
    }));

  });
});
